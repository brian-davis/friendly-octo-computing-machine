<p style="color: green"><%= notice %></p>

<b class="minor-heading"><%= title_line(@work) %></b>

<div id="<%= dom_id @work %>" class="outline">
  <%# DEBUG %>
  <% if @work.custom_citation.present? %>
    <div class="book-card outline bibliography-citation small-text">
      <%= raw markdown.render(@work.custom_citation) %>
    </div>
  <% end %>

  <% if @bibliography %>
    <div class="book-card outline bibliography-citation small-text">
      <%= raw markdown.render(@bibliography) %>
    </div>
  <% end %>

  <% if @work.parent_id %>
    <div>
    <span class="small-text"><%= @work.parent.format %>: <%= @work.parent.title %></span> <%= link_to "👉", @work.parent, class: "index-link" %>
    </div>
  <% end %>

  <p class="small-text">
    <%= alternate_title_line(@work) %>
  </p>

  <p class="small-text">
    <b>Language:</b> <%= language_line(@work) %>
  </p>

  <p class="small-text">
    <b>Rating:</b> <%= rating_stars(@work) %>
  </p>

    <p class="small-text">
    <b>Status:</b> <%= @work.finished? ? "Reading finished on #{human_time_for(@work.date_of_completion)}" : "Unread" %>
  </p>

  <% if @work.date_of_accession %>
    <p class="small-text">
      <b>Accession Date:</b> <%= human_time_for(@work.date_of_accession) %>
    </p>
  <% end %>

  <% if @work.accession_note.present? %>
    <p class="small-text">
      <b>Accession Note:</b> <%= @work.accession_note %>
    </p>
  <% end %>

  <hr>

  <div style="margin-bottom: 1em; cloud-list;">
    <b>Tags</b>
  </div>

  <div class="cloud-list">
    <% @work.tags.each do |tag| %>
      <%= link_to(works_path({ tag: tag }), style: "text-decoration: none;", data: { turbo_stream: true, action: "click->sortable#decorateTagClick", filter_value: tag }) do %>
        <span class="model-tag">
          <span class="model-tag-name">
            <%= tag %>
          </span>
        </span>
      <% end %>
    <% end %>
  </div>

  <hr>

  <div style="margin-bottom: 0.5rem;">
    <b>Authors</b>
  </div>

  <div class="associated-items">
    <% if @work.work_producers.empty? %>
      <div class="div-list-row empty-list">EMPTY</div>
    <% else %>
      <% @work_producers.each do |wp| %>
        <%= tag.div(**{ class: cycle("div-list-row", "div-list-row alt-row") }) do %>
          <span class="small-text"><%= wp.role.titleize.split(" ").join("-") %>: <%= wp.producer.full_name %></span>
          <%= link_to "👉", wp.producer, class: "index-link" %>
        <% end %>
      <% end %>
    <% end %>
  </div>

  <% if @work.children.any? %>
    <hr>
    <div style="margin-bottom: 0.5rem;">
      <b>Parts</b>
    </div>
    <div class="associated-items">
      <% if @work.children.empty? %>
        <div class="div-list-row empty-list">EMPTY</div>
      <% else %>
        <% @work.children.each do |child| %>
          <%= tag.div(**{ class: cycle("div-list-row", "div-list-row alt-row") }) do %>
            <span class="small-text"><%= child.format %>: <%= child.title %></span>
            <%= link_to "👉", child, class: "index-link" %>
          <% end %>
        <% end %>
      <% end %>
    </div>
  <% end %>

  <hr>

  <div style="margin-bottom: 0.5rem;">
    <b>Quotes</b>
    <%= link_to("👉", work_quotes_path(@work), class: "index-link") %>
  </div>
  <div>
    Count: <b class="stars"><%= @work.quotes.count %></b>
  </div>

  <hr>
  <div style="margin-bottom: 0.5rem;">
    <b>Notes</b>
    <%= link_to("👉", work_notes_path(@work), class: "index-link") %>
  </div>
  <div>
    Count: <b class="stars"><%= @work.notes.count %></b>
  </div>
  <hr>

  <div style="margin-bottom: 0.5rem;">
    <b>Reading Sessions</b>
    <%= link_to("👉", work_reading_sessions_path(@work), class: "index-link") %>
  </div>
  <div>
    Count: <b class="stars"><%= @work.reading_sessions.count %></b><br>
    Aggregate Time: <b class="stars"><%= human_duration(@work.reading_sessions_minutes) %></b>
  </div>
</div>

<hr>
<div class="nav-links" data-controller="confirmable">
  <%= link_to "Back to books", works_path, class: "nav-button" %>
  <%= link_to "Back to top", root_path, class: "nav-button" %>
  <%= link_to "Edit this work", edit_work_path(@work), class: "nav-button nav-action" %>
  <%= html_delete_button(@work, "Destroy Book") %>
</div>
