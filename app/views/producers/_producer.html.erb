<%# index views %>
<div id="<%= dom_id producer %>">
  <%= producer.name %> |
  <span class="stars"><%= (producer.works_count || 0) %></span> |
  <%= link_to "👉", producer, class: "index-link" %>
</div>